import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // this basically makes it so you can instantiate this class

  static const grisBase = Color.fromRGBO(204, 216, 208, 1);
  static const verdeBase = Color.fromRGBO(178, 219, 174, 1);
  static const verdeOscuro = Color.fromRGBO(000, 062, 021, 1);
  static const verdeClaroOscuro = Color.fromRGBO(063, 165, 053, 1);
  static const verdeOpacoOscuro = Color.fromRGBO(051, 101, 068, 1);
  static const blancoSimple = Color.fromRGBO(255, 255, 255, 1);
}
